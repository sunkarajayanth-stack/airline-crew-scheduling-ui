import { Bell } from 'lucide-react';import { useState } from 'react';import { AnimatePresence,motion } from 'framer-motion';
const items=['New crew leave request','Duty hour threshold reached','A320 roster published'];
export function Notifications(){const [open,setOpen]=useState(false);return <div className='relative'><button onClick={()=>setOpen(!open)} className='relative'><Bell/><span className='absolute -top-1 -right-1 text-[10px] bg-cyan-400 text-slate-900 rounded-full px-1'>3</span></button><AnimatePresence>{open&&<motion.div initial={{opacity:0,y:-6}} animate={{opacity:1,y:0}} exit={{opacity:0}} className='absolute right-0 top-10 w-72 glass rounded-xl p-3 z-20'>{items.map(i=><div key={i} className='py-2 border-b border-slate-700 last:border-0 text-sm'>{i}</div>)}</motion.div>}</AnimatePresence></div>}
